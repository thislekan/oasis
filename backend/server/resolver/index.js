import Query from './Query';
import Mutation from './Mutation';
import {
  Student,
  Course,
  CourseHistory,
  Faculty,
  Department,
} from './TypesResolver';

const resolvers = {
  Query,
  Mutation,
  Student,
  Course,
  CourseHistory,
  Faculty,
  Department,
};

export default resolvers;
